server:
  port: 63004

spring:
  application:
    name: bicyclestore-order-service
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: info, health, shutdown
  endpoint:
    shutdown:
      enabled: true

#logging:
#    config: classpath:logback.xml
